# 🚀 Starvell Crypto Currency Converter

Простой и быстрый сервис конвертации криптовалют через публичное API
**Cryptomus**, со встроенным кешированием (Redis + память), валидацией и
автогенерацией Swagger-документации.

Проект написан на **NestJS**, покрыт тестами, использует многослойную
архитектуру:\
`controller → service → provider → api → cache`.

## 🧱 Возможности

-   ⚡ Получение актуального курса криптовалюты\
-   🔁 Конвертация произвольной суммы\
-   🧩 Поддержка сотен валют Cryptomus\
-   🛡 Глобальная валидация параметров\
-   🧮 Кеширование (Redis + Memory) с TTL\
-   📦 Чистая модульная архитектура\
-   🔍 Swagger UI для документации\
-   🧪 Unit-тесты почти на весь код\
-   🐳 Поддержка Docker для полного окружения

## 📚 API Документация

Swagger доступен по адресу:

    http://localhost:3000/api/docs

## 🗂 Структура проекта

    /src
     ├── cryptonimus/
     ├── converter/
     ├── shared/
     └── main.ts

## ⚙️ Переменные окружения

### Локально:

    .env.local

### Docker:

    .env

### Пример .env.example

    PORT=3000
    CRYPTONIMUS_BASE_URL=https://api.cryptomus.com
    CRYPTONIMUS_VERSION=v1
    REDIS_HOST=redis
    REDIS_PORT=6379
    CACHE_TTL_MS=900000

## 🛠 Локальный запуск

    yarn
    yarn start:dev

## 🐳 Docker Compose

    docker compose up --build

## 🧪 Тесты

    yarn test
    yarn test:cov

## 🔍 Маршруты

### GET /converter/rate

    /converter/rate?from=ETH&to=USDT

### GET /converter

    /converter?from=ETH&to=USDT&amount=2

## 🧹 Кеширование

TTL задаётся через переменную:

    CACHE_TTL_MS

